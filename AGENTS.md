# 專案代理規則

本文件適用於整個儲存庫。子目錄若有更具體的 `AGENTS.md`，可在其範圍內補充或
收緊規則，但不得放寬本文件的一致性、安全及驗證要求。

## 多平台與多發佈版本一致性

本專案的 Claude Code、Codex 與 GitHub Copilot 實作，以及 copy-in、marketplace、
global install 等發佈型態，屬於同一產品的多個發佈版本。

修改任何功能、規則、hook、協定、skill、設定範本或操作流程時，不得只修改目前看到
的單一副本。施作前必須先盤點所有具有相同行為契約的版本，確認需要同步修改的範圍。

至少檢查：

- `shared/claude/`
- `shared/codex/`
- `claude/plugins/`
- `codex/plugins/`
- `copilot/plugins/`
- `decomposition-gate/`
- `harness/`
- `integrated-harness/`
- `.claude-plugin/`
- `.agents/`
- `scripts/`
- 對應的根層及模式內測試

## 一致性修改流程

1. 找出該功能的單一事實來源、發佈副本及所有使用者入口。
2. 建立「平台 × 模式 × 發佈型態」影響矩陣。
3. 同步修改所有具有相同行為契約的版本。
4. 某版本若不適用，必須在交付說明中列出並說明原因，不得靜默略過。
5. 新增或更新同步測試、行為一致性測試或安裝結構測試，避免日後再次漂移。
6. 執行受影響模式的專屬測試及根層完整回歸測試。
7. 更新實際受到影響的文件、`CHANGELOG.md` 及 Claude plugin 版號。

不得僅因檔名、程式語言或平台介面不同，就假設兩份實作不需同步；應以使用者可觀察
的行為契約判斷。

## 單一事實來源

已有 `shared/` 單一事實來源或同步腳本的功能，必須先修改單一事實來源，再使用既有
同步流程產生發佈副本；不得直接手動修改產生副本。

尚未建立單一事實來源的重複資產，修改時至少加入逐位元組同步測試。若各平台因介面
差異無法逐位元組一致，則加入涵蓋共同輸入與預期結果的行為一致性測試。

## 文件與版本

修改核心邏輯、公開介面或操作方式時，至少檢查：

- `README.md`
- `CLI_REFERENCE.md`
- `ARCHITECTURE.md`
- `CHANGELOG.md`

Claude plugin 的行為有變更時，更新其 `.claude-plugin/plugin.json` 版號。Codex 與
Copilot 沒有 plugin 版號欄位，不得另造無平台承載的版本號；發布座標與版本語意遵循
`VERSIONING.md`。

## 專案記憶

回答架構問題、開始新功能，或使用者提到先前決策時，先讀取專案內
`.docs/vault/` 的相關筆記。

做出具有持續影響的架構決策時，寫入 `.docs/vault/decisions/`；解決容易重現且有
重用價值的陷阱時，寫入 `.docs/vault/gotchas/`。不同專案的 vault 不得混用。

## 完成交付要求

最終回報必須包含：

- 已修改的平台、模式及發佈型態
- 經檢查但不適用的版本及原因
- 已執行的同步、專屬與完整回歸測試
- 尚未驗證的環境或剩餘一致性風險

沒有完成影響矩陣與一致性驗證，不得宣告功能變更完成。
