---
name: harness
description: Require plan approval and apply command and secret guardrails.
---

# Harness

This workflow gates implementation on plan approval and applies command and credential checks. Plugin hooks are not a sandbox and installation alone does not activate them.

Activate this mode with `scripts/select-codex-mode harness`, then start a new thread after switching.
